// Example environment variables for the frontend (Vite)
// Copy this to `.env` or set these in your environment for development

# Base URL for API. When unset, the app will use http://localhost:5000 in dev
# and https://ekahera.onrender.com in production.
VITE_API_BASE_URL=http://localhost:5000

# Example: If you use Supabase or other services, add keys here
# VITE_SUPABASE_ANON_KEY=your_key_here
